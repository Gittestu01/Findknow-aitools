# 核心框架
streamlit>=1.28.0,<1.50.0

# AI和API相关
openai>=1.0.0,<2.0.0
httpx>=0.24.0,<1.0.0

# 数据处理
pandas>=2.0.0,<3.0.0
numpy>=1.24.0,<2.0.0

# 图像和视频处理
opencv-python-headless>=4.8.0,<5.0.0
Pillow>=10.0.0,<12.0.0

# 文件处理
PyPDF2>=3.0.0
python-docx>=0.8.11
openpyxl>=3.1.0

# 网络请求
requests>=2.31.0

# 数据可视化
plotly>=5.0.0

# 系统和配置
python-dotenv>=1.0.0
pathlib2>=2.3.0; python_version<"3.4"

# 日期时间处理（用于文件名生成等）
python-dateutil>=2.8.0

# JSON处理增强（某些情况下需要）
ujson>=5.0.0; platform_system!="Windows"

# 开发和调试工具（可选）
# streamlit-option-menu>=0.3.0

# 注意事项：
# 1. opencv-python-headless 用于无GUI环境（云部署）
# 2. 如果本地开发需要GUI功能，可安装 opencv-python
# 3. Python版本建议：3.8-3.11（避免3.13的兼容性问题）
# 4. ujson在Windows环境下可能有编译问题，故排除
# 5. pathlib2仅在Python<3.4时需要（实际已不常见）
